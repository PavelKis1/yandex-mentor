# Задания: Mocks

## Задание 1: Алгоритм на собесе
Решите Two Sum за O(n) на бумаге: объясните подход, напишите код, укажите сложность.

**Пример:** `дикт: {число: индекс}, O(n)`

💡 **Подсказка:** Объясни до кода.

## Задание 2: Ещё задача
Найдите k-й наибольший элемент в массиве за O(n log k) (heap).

**Пример:** `heapq.nlargest / min-heap размера k`

💡 **Подсказка:** Объясни выбор структуры.

## Задание 3: Обсуждение
Расскажите про разницу list vs dict по времени операций.

**Пример:** `list: O(n) поиск, O(1) append; dict: O(1) get/set`

💡 **Подсказка:** Говори про амортизацию.
