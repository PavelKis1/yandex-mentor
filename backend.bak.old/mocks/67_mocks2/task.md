# Задания: Mocks2

## Задание 1: Вопрос по Python
Что выведет и почему: def f(lst=[]): lst.append(1); return lst — для двух вызовов f()?

**Пример:** `[1], затем [1,1] — mutable default`

💡 **Подсказка:** Мутабельный дефолтный аргумент.

## Задание 2: Декоратор на собесе
Напишите декоратор memoize на бумаге для рекурсивной функции.

**Пример:** `кэш {args: result}`

💡 **Подсказка:** functools.lru_cache суть.

## Задание 3: Беседа
Как бы вы профилировали медленную функцию продакшена?

**Пример:** `cProfile/трассировка + гипотезы`

💡 **Подсказка:** Сначала измерь, потом оптимизируй.
