{
  "id": "05-p1",
  "title": "Разворот списка",
  "difficulty": "easy",
  "lecture_id": "05",
  "order": 1,
  "description": "Дан односвязный список. Разверните его и верните новый head. Задачу решите за O(n) по времени и O(1) по дополнительной памяти (без использования массива/стека).\n\nСписок передан как узел `ListNode` (`val`, `next`). В тестах он создаётся из массива значений, а результат сравнивается как список.\n\n**Функция решения:**\n\n```python\ndef reverse_list(head: ListNode | None) -> ListNode | None:\n    ...\n```",
  "examples": [
    {"input": "head = [1, 2, 3, 4, 5]", "output": "[5, 4, 3, 2, 1]", "explanation": "Порядок узлов обращается."},
    {"input": "head = []", "output": "[]", "explanation": "Пустой список остаётся пустым."},
    {"input": "head = [5]", "output": "[5]", "explanation": "Список из одного узла не меняется."}
  ],
  "constraints": [
    "0 ≤ количество узлов ≤ 5·10^3",
    "−10^3 ≤ val узла ≤ 10^3"
  ],
  "hints": [
    "Идите по списку, сохраняя указатель на предыдущий узел.",
    "Перед перевешиванием cur.next сохраните следующий узел, чтобы не потерять список.",
    "Итеративная версия — O(n) по времени и O(1) по памяти."
  ],
  "starter_code": "class ListNode:\n    \"\"\"Узел односвязного списка.\"\"\"\n    def __init__(self, val: int = 0, next: \"ListNode | None\" = None) -> None:\n        self.val = val\n        self.next = next\n\n\ndef reverse_list(head: ListNode | None) -> ListNode | None:\n    \"\"\"Развернуть односвязный список.\"\"\"\n    ...\n\n\ndef _build(values: list[int]) -> ListNode | None:\n    dummy = ListNode()\n    tail = dummy\n    for value in values:\n        tail.next = ListNode(value)\n        tail = tail.next\n    return dummy.next\n\n\ndef _to_list(head: ListNode | None) -> list[int]:\n    result: list[int] = []\n    while head is not None:\n        result.append(head.val)\n        head = head.next\n    return result\n",
  "entry_function": "reverse_list",
  "test_cases": [
    {"id": "t1", "args": [[1, 2, 3, 4, 5]], "expected": [5, 4, 3, 2, 1], "hidden": false, "arg_converters": ["_build"], "result_converter": "_to_list"},
    {"id": "t2", "args": [[]], "expected": [], "hidden": false, "arg_converters": ["_build"], "result_converter": "_to_list"},
    {"id": "t3", "args": [[5]], "expected": [5], "hidden": false, "arg_converters": ["_build"], "result_converter": "_to_list"},
    {"id": "t4", "args": [[1, 2]], "expected": [2, 1], "hidden": true, "arg_converters": ["_build"], "result_converter": "_to_list"},
    {"id": "t5", "args": [[-3, 0, 3]], "expected": [3, 0, -3], "hidden": true, "arg_converters": ["_build"], "result_converter": "_to_list"}
  ],
  "timeout_ms": 3000
}