{
  "id": "05-p3",
  "title": "Обнаружение цикла",
  "difficulty": "hard",
  "lecture_id": "05",
  "order": 3,
  "description": "Определите, есть ли цикл в списке.\n\n**Функция решения:**\n\n```python\ndef has_cycle(head: ListNode | None) -> bool:\n    \"\"\"O(n) — алгоритм Флойда «черепаха и заяц».\n\n    Edge cases: пустой список, цикл из одного узла, цикл в конце списка.\n    \"\"\"\n```",
  "examples": [],
  "constraints": [],
  "hints": [],
  "starter_code": "class ListNode:\n    \"\"\"Узел односвязного списка.\"\"\"\n\n    def __init__(self, val: int = 0, next: ListNode | None = None) -> None:\n        self.val = val\n        self.next = next\n\ndef has_cycle(head: ListNode | None) -> bool:\n    \"\"\"O(n) — алгоритм Флойда «черепаха и заяц».\n\n    Edge cases: пустой список, цикл из одного узла, цикл в конце списка.\n    \"\"\"\n    ...\n\ndef _build(values: list[int]) -> ListNode | None:\n    dummy = ListNode()\n    tail = dummy\n    for value in values:\n        tail.next = ListNode(value)\n        tail = tail.next\n    return dummy.next\n\ndef _to_list(head: ListNode | None) -> list[int]:\n    result: list[int] = []\n    while head is not None:\n        result.append(head.val)\n        head = head.next\n    return result\n",
  "entry_function": "has_cycle",
  "test_cases": [
    {
      "id": "t1",
      "args": [
        []
      ],
      "expected": false,
      "hidden": false,
      "arg_converters": [
        "_build"
      ],
      "result_converter": null
    }
  ],
  "timeout_ms": 3000
}