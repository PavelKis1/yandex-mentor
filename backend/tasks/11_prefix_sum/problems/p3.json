{
  "id": "11-p3",
  "title": "Подмножество с суммой",
  "difficulty": "hard",
  "lecture_id": "11",
  "order": 3,
  "description": "Найдите количество подмножеств с суммой k.\n\n**Функция решения:**\n\n```python\ndef count_subsets(nums: list[int], k: int) -> int:\n    \"\"\"O(n*k) — DP по сумме (рюкзак 0/1), нижняя граница по памяти.\n\n    Edge cases: пустой массив (k=0 даёт 1), один элемент, нулевой суммовой лимит.\n    \"\"\"\n```",
  "examples": [],
  "constraints": [],
  "hints": [],
  "starter_code": "def count_subsets(nums: list[int], k: int) -> int:\n    \"\"\"O(n*k) — DP по сумме (рюкзак 0/1), нижняя граница по памяти.\n\n    Edge cases: пустой массив (k=0 даёт 1), один элемент, нулевой суммовой лимит.\n    \"\"\"\n    ...\n",
  "entry_function": "count_subsets",
  "test_cases": [
    {
      "id": "t1",
      "args": [
        [
          1,
          2,
          3
        ],
        3
      ],
      "expected": 2,
      "hidden": false,
      "arg_converters": [
        null,
        null
      ],
      "result_converter": null
    },
    {
      "id": "t2",
      "args": [
        [],
        0
      ],
      "expected": 1,
      "hidden": false,
      "arg_converters": [
        null,
        null
      ],
      "result_converter": null
    },
    {
      "id": "t3",
      "args": [
        [
          1,
          2,
          3
        ],
        7
      ],
      "expected": 0,
      "hidden": false,
      "arg_converters": [
        null,
        null
      ],
      "result_converter": null
    },
    {
      "id": "t4",
      "args": [
        [
          0,
          1
        ],
        1
      ],
      "expected": 2,
      "hidden": false,
      "arg_converters": [
        null,
        null
      ],
      "result_converter": null
    }
  ],
  "timeout_ms": 3000
}